spring:
  profiles:
    include: secret

  datasource:
    url:
    username:
    password:
    driver-class-name:

  jpa:
    hibernate:
      ddl-auto:
    properties:
      hibernate:
        format_sql:
        dialect:

  jwt:
    header:
    secret:
    access-token-validity-in-seconds :
    refresh-token-validity-in-seconds :
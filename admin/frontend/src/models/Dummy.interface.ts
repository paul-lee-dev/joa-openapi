export interface IDummy {
  dummyId: string;
  adminId: string;
  name: string;
  memberCount: number | null;
  accountCount: number | null;
  transactionCount: number | null;
  createdAt: string;
  updatedAt: string;
}
